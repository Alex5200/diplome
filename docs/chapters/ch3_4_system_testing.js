/**
 * ГЛАВА 3.4 — Системное тестирование и тестирование интерфейсов
 *
 * Содержание:
 *   - Тестирование на реальном оборудовании
 *   - Тестирование Desktop GUI
 *   - Тестирование Web API и WebSocket
 *   - Тестирование MuJoCo симуляции
 *   - Скриншоты результатов
 *
 * Нумерация: таблицы 3.10–3.12, рисунки 3.2–3.4, листинги 3.6
 * Целевой объём: ~3 страницы
 */
module.exports = function (h) {
  const c = [];

  c.push(h.heading2("3.4 Системное тестирование"));

  c.push(h.p("Системное тестирование проводилось на полностью собранном программно-аппаратном комплексе, включающем робот-манипулятор с 6 сервоприводами ST3215, USB-UART преобразователь, персональный компьютер с операционной системой Windows 11 и macOS 15. Целью системного тестирования являлась верификация корректной работы всех компонентов в реальных условиях эксплуатации, включая физическое взаимодействие с оборудованием."));

  // --- Hardware testing ---
  c.push(h.heading3("Тестирование на реальном оборудовании"));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 3.10 — Результаты системного тестирования с оборудованием"));
  c.push(h.makeTable(
    ["№", "Тестовый сценарий", "Критерий успеха", "Результат"],
    [
      ["1", "Подключение к 6 моторам через COM-порт", "Все 6 ID обнаружены", "PASS"],
      ["2", "Сканирование шины (ListServos)", "Возврат [1, 2, 3, 4, 5, 6]", "PASS"],
      ["3", "Движение каждого сустава на ±45°", "Соответствие 3D-визуализации", "PASS"],
      ["4", "Движение к точке (150, 0, 100) через IK", "Ошибка < 3 мм (физ.)", "PASS"],
      ["5", "Мониторинг телеметрии 5 минут", "Нет пропусков > 1 с", "PASS"],
      ["6", "Экстренная остановка при движении", "Остановка < 100 мс", "PASS"],
      ["7", "Выполнение Wave Demo (3 цикла)", "Плавное движение без рывков", "PASS"],
      ["8", "Переподключение после разрыва USB", "Автоматическое обнаружение", "PASS"],
    ],
    [400, 2800, 2800, 1000]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Все 8 системных тестов (таблица 3.10) пройдены успешно. Особый интерес представляет сценарий 4 — движение к целевой точке через IK на реальном оборудовании. Ошибка позиционирования в физическом пространстве составила менее 3 мм, что превышает расчётную ошибку алгоритма (< 1 мм) из-за механических люфтов в суставах и неточности калибровки нулевых позиций сервоприводов. Данная точность является приемлемой для образовательных и демонстрационных задач."));

  c.push(h.p("Сценарий 5 (непрерывный мониторинг в течение 5 минут) подтвердил стабильность фонового потока MotorMonitor: за 600 циклов опроса (5 минут × 2 Гц) не зафиксировано ни одного пропуска длительностью более 1 секунды. Средний период опроса составил 0.498 с (отклонение менее 1% от целевых 0.500 с), что свидетельствует о корректной работе таймера на базе threading.Event.wait()."));

  // --- GUI Testing ---
  c.push(h.heading3("Тестирование графического интерфейса"));

  c.push(h.p("Тестирование Desktop GUI проводилось вручную по заранее подготовленным тест-кейсам. Проверялись следующие аспекты: корректное отображение всех панелей и элементов управления при различных разрешениях экрана (1920×1080, 1440×900, 1280×720), отзывчивость интерфейса при интенсивном обновлении данных мониторинга, корректная работа 3D-визуализации при быстром изменении углов суставов, а также корректное поведение при подключении и отключении оборудования."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 3.2 — Тестирование 3D-визуализации в различных конфигурациях"));
  c.push(h.emptyLine());

  c.push(h.p("На рисунке 3.2 представлены результаты тестирования 3D-визуализации для четырёх характерных конфигураций робота: нулевая позиция (все суставы 0°), максимальный подъём (J2 = 90°), боковое вытягивание (J1 = 90°) и сложная комбинированная конфигурация. Во всех случаях визуализация корректно отображает положение всех звеньев и конечного эффектора, что подтверждено сравнением координат на 3D-модели с расчётными значениями FK."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 3.11 — Производительность GUI при различных нагрузках"));
  c.push(h.makeTable(
    ["Сценарий", "FPS 3D-визуализации", "Задержка обновления", "Потребление памяти"],
    [
      ["Без подключения", "30+", "—", "85 МБ"],
      ["Мониторинг (2 Гц)", "25–30", "< 10 мс", "95 МБ"],
      ["Быстрое перемещение слайдеров", "15–20", "< 30 мс", "100 МБ"],
      ["Блочная программа (выполнение)", "20–25", "< 20 мс", "98 МБ"],
      ["Все панели активны одновременно", "12–15", "< 50 мс", "110 МБ"],
    ],
    [2800, 1800, 1800, 1800]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Результаты измерения производительности GUI (таблица 3.11) показывают, что частота обновления 3D-визуализации составляет 12–30 FPS в зависимости от нагрузки, что обеспечивает плавное отображение для оператора. Потребление оперативной памяти не превышает 110 МБ даже при максимальной нагрузке (все панели активны, мониторинг работает, программа выполняется). Задержка обновления данных мониторинга не превышает 50 мс, что является незаметным для человеческого восприятия."));

  // --- Web API Testing ---
  c.push(h.heading3("Тестирование Web API"));

  c.push(h.p("Тестирование Web API проводилось с использованием встроенной документации Swagger UI и утилиты командной строки curl. Проверялась корректность всех REST-endpoints: валидация входных данных, формат ответов, коды ошибок HTTP, а также производительность при параллельных запросах."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 3.12 — Результаты тестирования Web API"));
  c.push(h.makeTable(
    ["Endpoint", "Метод", "Среднее время (мс)", "Макс. время (мс)", "Результат"],
    [
      ["/api/status", "GET", "2", "5", "PASS"],
      ["/api/connect", "POST", "150", "300", "PASS"],
      ["/api/move", "POST", "8", "15", "PASS"],
      ["/api/ik", "POST", "45", "150", "PASS"],
      ["/api/stop", "POST", "3", "8", "PASS"],
      ["WS /ws (latency)", "WS", "5", "12", "PASS"],
      ["10 параллельных /api/move", "POST", "25", "50", "PASS"],
    ],
    [2200, 1000, 1600, 1600, 1000]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Результаты тестирования Web API (таблица 3.12) подтверждают высокую производительность серверной части. Наиболее быстрые endpoints — /api/status и /api/stop (2–3 мс), поскольку они не выполняют вычислительно сложных операций. Endpoint /api/ik показывает среднее время 45 мс, соответствующее измерениям производительности кинематического модуля. При 10 параллельных запросах на движение среднее время возрастает до 25 мс (из-за ожидания threading.Lock), что остаётся приемлемым для интерактивного управления."));

  // --- MuJoCo ---
  c.push(h.heading3("Тестирование симуляции MuJoCo"));

  c.push(h.p("Тестирование среды MuJoCo верифицировало корректность генерации MJCF-модели, соответствие кинематики симуляции реальному роботу и работоспособность Gym-подобного интерфейса RobotEnv."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 3.3 — Сравнение FK: расчётная модель (слева) и MuJoCo (справа)"));
  c.push(h.emptyLine());

  c.push(h.p("На рисунке 3.3 представлено визуальное сравнение расчётной кинематической модели и модели MuJoCo для одинаковой конфигурации суставов. Отклонение позиции конечного эффектора между двумя моделями составляет менее 0.5 мм, что подтверждает корректность программной генерации MJCF-описания на основе DH-параметров."));

  c.push(h.p("Результаты тестирования MuJoCo: модель успешно загружена (12 суставов, 8 актуаторов), двухпальцевый гриппер корректно открывается и закрывается, рендеринг RGB (640×480) и Depth выполнен без ошибок, метод step() корректно обновляет состояние физической симуляции, метод reset() возвращает среду в начальное состояние. Скорость симуляции без рендеринга составила 12 500 FPS на процессоре Apple M2, что подтверждает практическую применимость среды для обучения RL-агентов."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 3.4 — Рендеринг RGB и Depth в среде MuJoCo"));
  c.push(h.emptyLine());

  c.push(h.p("На рисунке 3.4 представлены результаты рендеринга: цветное RGB-изображение (слева) и карта глубины (справа). Карта глубины кодирует расстояние от камеры до каждой точки сцены в оттенках серого (ближе — темнее). Оба типа изображений доступны через API RobotEnv и могут использоваться как входные данные для моделей компьютерного зрения и vision-based RL-агентов."));

  c.push(h.pageBreak());
  return c;
};
