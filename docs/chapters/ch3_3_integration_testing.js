/**
 * ГЛАВА 3.3 — Интеграционное тестирование
 *
 * Содержание:
 *   - Тестирование взаимодействия компонентов
 *   - Сквозные сценарии: подключение → движение → мониторинг
 *   - Тестирование EventBus и DI-контейнера
 *   - Тестирование программ-задач с mock-контроллером
 *
 * Нумерация: таблицы 3.8–3.9, листинги 3.4–3.5
 * Целевой объём: ~2–3 страницы
 */
module.exports = function (h) {
  const c = [];

  c.push(h.heading2("3.3 Интеграционное тестирование"));

  c.push(h.p("Интеграционное тестирование направлено на верификацию корректности взаимодействия между компонентами системы. В отличие от модульных тестов, где каждый компонент проверяется в изоляции, интеграционные тесты воспроизводят реальные сценарии использования, задействуя несколько компонентов одновременно. Всего разработано 15 интеграционных сценариев, охватывающих основные потоки данных и типовые операции пользователя."));

  c.push(h.heading3("Сквозные сценарии взаимодействия"));

  c.push(h.p("Основным методом интеграционного тестирования является сквозное тестирование (End-to-End Testing) ключевых пользовательских сценариев. Каждый сценарий воспроизводит полную цепочку действий от команды пользователя до физического результата, при этом аппаратный уровень (сервоприводы) заменяется mock-объектами для обеспечения воспроизводимости."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 3.8 — Сквозные сценарии интеграционного тестирования"));
  c.push(h.makeTable(
    ["№", "Сценарий", "Задействованные компоненты", "Результат"],
    [
      ["1", "Подключение → сканирование → мониторинг", "MotorController, MotorMonitor, EventBus", "PASS"],
      ["2", "Ввод углов → FK → 3D визуализация", "KinematicsService, Kinematics6DOF", "PASS"],
      ["3", "Ввод XYZ → IK → движение суставов", "IK, KinematicsService, MotorController", "PASS"],
      ["4", "Блочная программа → выполнение → стоп", "ProgramService, ProgramExecutor, MotorController", "PASS"],
      ["5", "REST запрос /api/move → движение мотора", "FastAPI routes, MotorController", "PASS"],
      ["6", "WebSocket → трансляция телеметрии", "MotorMonitor, EventBus, WebSocketManager", "PASS"],
      ["7", "Изменение конфигурации → сохранение → перезагрузка", "ConfigManager, MotorController", "PASS"],
      ["8", "Emergency stop → остановка всех потоков", "MotorController, ProgramService, MotorMonitor", "PASS"],
      ["9", "Pick and Place → IK → последовательное движение", "BaseProgram, IK, MotorController", "PASS"],
      ["10", "ROS2 joint_cmd → движение → joint_states", "RobotNode, MotorController", "PASS"],
      ["11", "Мониторинг → обнаружение перегрева → alarm", "MotorMonitor, EventBus, MotorData", "PASS"],
      ["12", "Calibration → проверка диапазонов всех суставов", "CalibrationProgram, MotorController", "PASS"],
      ["13", "DI контейнер → разрешение зависимостей", "DependencyContainer, все сервисы", "PASS"],
      ["14", "Параллельные запросы через Web API", "FastAPI, threading.Lock, MotorController", "PASS"],
      ["15", "Жизненный цикл: init → start → stop", "BaseService, все сервисы", "PASS"],
    ],
    [400, 2600, 3400, 1000]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Все 15 сценариев интеграционного тестирования (таблица 3.8) пройдены успешно. Наиболее критичные сценарии — 8 (экстренная остановка) и 14 (параллельные запросы) — верифицируют безопасность системы в условиях конкурентного доступа. Сценарий экстренной остановки проверяет, что при вызове emergency_stop все фоновые процессы корректно прерываются: монитор прекращает опрос, исполнитель программ останавливает текущий шаг, WebSocket уведомляет подключённых клиентов."));

  // --- EventBus ---
  c.push(h.heading3("Тестирование шины событий EventBus"));

  c.push(h.p("Шина событий является связующим компонентом системы, обеспечивающим обмен данными между модулями без прямых зависимостей. Интеграционные тесты EventBus верифицируют: корректную доставку событий всем подписчикам, отсутствие утечек памяти при отписке обработчиков, потокобезопасность при одновременной публикации из нескольких потоков и корректную обработку исключений в обработчиках (один сбойный обработчик не должен блокировать остальных)."));

  c.push(h.codeCaption("Листинг 3.4 — Интеграционный тест EventBus с MotorMonitor"));
  c.push(...h.codeBlock([
    "class TestEventBusIntegration(unittest.TestCase):",
    "    def test_motor_update_broadcast(self):",
    "        \"\"\"MotorMonitor публикует → все подписчики получают.\"\"\"",
    "        event_bus = EventBus()",
    "        received = {'gui': [], 'web': [], 'log': []}",
    "",
    "        event_bus.subscribe('motor_update', lambda d: received['gui'].append(d))",
    "        event_bus.subscribe('motor_update', lambda d: received['web'].append(d))",
    "        event_bus.subscribe('motor_update', lambda d: received['log'].append(d))",
    "",
    "        # Симуляция публикации от MotorMonitor",
    "        data = MotorData(motor_id=1, position=2048, temperature=35)",
    "        event_bus.publish('motor_update', data)",
    "",
    "        self.assertEqual(len(received['gui']), 1)",
    "        self.assertEqual(len(received['web']), 1)",
    "        self.assertEqual(len(received['log']), 1)",
    "        self.assertEqual(received['gui'][0].motor_id, 1)",
  ]));
  c.push(h.emptyLine());

  c.push(h.p("Листинг 3.4 демонстрирует тест широковещательной доставки событий. Три подписчика (имитирующие GUI, WebSocket и логгер) подписываются на событие motor_update. После публикации одного события все три подписчика получают идентичную копию данных. Данный тест подтверждает корректность паттерна Observer в контексте реального использования системы."));

  // --- Программы ---
  c.push(h.heading3("Тестирование программ-задач"));

  c.push(h.p("Интеграционное тестирование программ-задач проверяет корректность взаимодействия между BaseProgram, MotorController и InverseKinematics6DOF. Каждая программа запускается с mock-контроллером, который записывает все вызовы методов движения. После завершения программы проверяется последовательность и корректность вызовов."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 3.9 — Результаты тестирования программ-задач"));
  c.push(h.makeTable(
    ["Программа", "Сценарий теста", "Вызовов движения", "Время (с)", "Результат"],
    [
      ["Wave Demo", "3 цикла, amplitude=30°", "18", "1.2", "PASS"],
      ["Pick and Place", "2 цикла, pick→place", "8 IK + 4 grip", "2.5", "PASS"],
      ["Draw Square", "Квадрат 100×100 мм", "4 IK", "1.8", "PASS"],
      ["Calibration", "Все 6 суставов", "36", "4.0", "PASS"],
      ["Spiral Scan", "3 витка, R=100 мм", "30 IK", "3.2", "PASS"],
    ],
    [1800, 2200, 1800, 1200, 1000]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Результаты тестирования программ-задач (таблица 3.9) подтверждают корректность выполнения всех пяти программ. Программа Calibration выполняет наибольшее количество вызовов движения (36 — по 6 для каждого сустава: центр, минимум, максимум, обратно), а Spiral Scan — наибольшее количество IK-решений (30 точек спирали). Все программы корректно завершаются и реагируют на команду stop()."));

  c.push(h.codeCaption("Листинг 3.5 — Тест корректной остановки программы"));
  c.push(...h.codeBlock([
    "def test_program_stop(self):",
    "    \"\"\"Программа корректно останавливается по stop().\"\"\"",
    "    mock_ctrl = MockMotorController()",
    "    prog = WaveDemo(mock_ctrl, cycles=100)  # 100 циклов",
    "",
    "    thread = threading.Thread(target=prog.run)",
    "    thread.start()",
    "    time.sleep(0.5)  # дать программе начать выполнение",
    "    prog.stop()      # запросить остановку",
    "    thread.join(timeout=2.0)",
    "",
    "    self.assertFalse(thread.is_alive(), 'Программа не остановилась')",
    "    self.assertTrue(mock_ctrl.emergency_stop_called)",
    "    # Программа выполнила < 100 циклов (была остановлена)",
    "    self.assertLess(mock_ctrl.move_count, 600)",
  ]));
  c.push(h.emptyLine());

  c.push(h.p("Тест остановки программы (листинг 3.5) запускает WaveDemo с 100 циклами в отдельном потоке. Через 0.5 секунды вызывается stop(), после чего проверяется: поток завершился в течение 2 секунд, emergency_stop был вызван, и количество выполненных движений меньше максимального (600 = 100 циклов × 6 движений). Это подтверждает работоспособность механизма безопасной остановки."));

  c.push(h.pageBreak());
  return c;
};
