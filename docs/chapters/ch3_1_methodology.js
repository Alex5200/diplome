/**
 * ГЛАВА 3.1 — Методология тестирования
 *
 * Содержание:
 *   - Стратегия тестирования: уровни, инструменты
 *   - Классификация тестов
 *   - Среда тестирования
 *   - Метрики качества
 *
 * Нумерация: таблицы 3.1–3.2, рисунки 3.1
 * Целевой объём: ~2 страницы
 */
module.exports = function (h) {
  const c = [];

  c.push(h.heading1("ГЛАВА 3. ТЕСТИРОВАНИЕ И РЕЗУЛЬТАТЫ"));

  c.push(h.heading2("3.1 Методология и стратегия тестирования"));

  c.push(h.p("Тестирование программного обеспечения для управления роботом-манипулятором имеет ряд особенностей, обусловленных взаимодействием с физическим оборудованием. Невозможность полной автоматизации аппаратных тестов, необходимость обеспечения безопасности при тестировании на реальном роботе и сложность воспроизведения граничных условий требуют многоуровневого подхода к верификации. В данном разделе описана стратегия тестирования, применённая при разработке системы, классификация тестов по уровням и метрики оценки качества."));

  c.push(h.heading3("Уровни тестирования"));

  c.push(h.p("Стратегия тестирования построена по модели «тестовая пирамида» (Test Pyramid), включающей четыре уровня: модульное тестирование (Unit Testing), интеграционное тестирование (Integration Testing), системное тестирование (System Testing) и приёмочное тестирование (Acceptance Testing). Каждый уровень имеет свои цели, инструменты и критерии успешности."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 3.1 — Уровни тестирования системы"));
  c.push(h.makeTable(
    ["Уровень", "Объём", "Инструменты", "Цель", "Автоматизация"],
    [
      ["Модульное", "83 теста", "unittest, mock", "Корректность отдельных классов и методов", "Полная"],
      ["Интеграционное", "15 сценариев", "unittest, mock", "Взаимодействие между компонентами", "Полная"],
      ["Системное", "8 сценариев", "Ручное + скрипты", "Работа всей системы с оборудованием", "Частичная"],
      ["Приёмочное", "5 задач", "Ручное", "Выполнение типовых рабочих сценариев", "Нет"],
    ],
    [1600, 1400, 1800, 2800, 1400]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Как видно из таблицы 3.1, основной объём автоматизированных тестов приходится на модульный уровень (83 теста), что соответствует принципу тестовой пирамиды: наибольшее количество быстрых и изолированных тестов на нижнем уровне, меньшее количество более сложных тестов на верхних уровнях. Общее время выполнения всех модульных тестов составляет менее 3 секунд, что позволяет запускать их при каждом изменении кода."));

  // --- Классификация ---
  c.push(h.heading3("Классификация тестов по компонентам"));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 3.2 — Распределение тестов по компонентам"));
  c.push(h.makeTable(
    ["Компонент", "Файл теста", "Количество тестов", "Покрытие (%)"],
    [
      ["RobotKinematics6DOF", "test_kinematics.py", "24", "95"],
      ["InverseKinematics6DOF", "test_kinematics.py", "12", "90"],
      ["MotorController", "test_motor_controller.py", "25", "88"],
      ["MotorMonitor", "test_motor_controller.py", "11", "85"],
      ["MotorData", "test_motor_controller.py", "8", "92"],
      ["ConfigManager", "test_motor_controller.py", "3", "75"],
      ["Итого", "—", "83", "89 (среднее)"],
    ],
    [2600, 2400, 1800, 1800]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Таблица 3.2 показывает распределение тестов по компонентам системы. Наибольшее количество тестов приходится на кинематический модуль (36 тестов) и контроллер моторов (25 тестов), что отражает критическую важность этих компонентов для корректной работы системы. Среднее покрытие кода тестами составляет 89%, что превышает рекомендуемый порог 80% для промышленного ПО."));

  // --- Среда тестирования ---
  c.push(h.heading3("Среда тестирования"));

  c.push(h.p("Тестирование выполнялось в следующей среде: операционная система Windows 11 и macOS 15 (Sequoia), Python 3.12.7, фреймворк unittest из стандартной библиотеки Python, библиотека unittest.mock для создания mock-объектов. Для модульных тестов использовались mock-объекты, имитирующие поведение реальных сервоприводов ST3215, что обеспечивает воспроизводимость и независимость от наличия оборудования."));

  c.push(h.p("Для тестирования на реальном оборудовании использовался робот-манипулятор с 6 сервоприводами ST3215, подключённый через USB-UART преобразователь на скорости 1 000 000 бод. Системные и приёмочные тесты проводились в безопасном режиме с ограничением скорости до 100 шагов/с и безопасными диапазонами углов."));

  // --- Метрики ---
  c.push(h.heading3("Метрики качества"));

  c.push(h.p("Для оценки качества разработанного ПО использовались следующие метрики:"));

  c.push(h.bulletDash("покрытие кода тестами (Code Coverage) — процент строк кода, выполненных при прогоне тестов;"));
  c.push(h.bulletDash("точность позиционирования — ошибка между целевой и фактической позицией конечного эффектора в миллиметрах;"));
  c.push(h.bulletDash("время отклика — задержка между командой и началом движения мотора;"));
  c.push(h.bulletDash("стабильность мониторинга — отсутствие пропусков в потоке телеметрических данных;"));
  c.push(h.bulletDash("надёжность IK — процент успешно решённых задач обратной кинематики для точек в рабочем пространстве;"));
  c.push(h.bulletDash("производительность GUI — частота обновления 3D-визуализации без задержек."));

  c.push(h.p("Целевые значения метрик определены на основе требований к промышленным системам управления манипуляторами: точность позиционирования менее 1 мм, время отклика менее 50 мс, надёжность IK не менее 95% в пределах рабочего пространства, частота обновления GUI не менее 10 FPS."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 3.1 — Тестовая пирамида проекта"));
  c.push(h.emptyLine());

  c.push(h.p("Рисунок 3.1 визуализирует тестовую пирамиду проекта: 83 модульных теста на основании, 15 интеграционных тестов в середине, 8 системных сценариев и 5 приёмочных задач на вершине. Пропорции соответствуют рекомендациям Мартина Фаулера для проектов с критической бизнес-логикой."));

  c.push(h.pageBreak());
  return c;
};
