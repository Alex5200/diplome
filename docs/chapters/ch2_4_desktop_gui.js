/**
 * ГЛАВА 2.4 — Desktop GUI интерфейс
 *
 * Содержание:
 *   - Архитектура GUI на Tkinter
 *   - FANUC-стилизация (тёмная тема)
 *   - Вкладки: ручное управление, 3D-кинематика, блочное программирование, мониторинг
 *   - 3D-визуализация через Matplotlib
 *   - Скриншоты интерфейса
 *
 * Нумерация: таблицы 2.6–2.7, рисунки 2.4–2.7, листинги 2.8–2.9
 * Целевой объём: ~3 страницы
 */
module.exports = function (h) {
  const c = [];

  c.push(h.heading2("2.4 Desktop-приложение с графическим интерфейсом"));

  c.push(h.p("Desktop-слой (директория desktop/, 3927 строк кода в app/views/) предназначен для прямого управления роботом-манипулятором с компьютера оператора через USB-кабель. Графический интерфейс реализован на библиотеке Tkinter, входящей в стандартную поставку Python, что исключает необходимость установки дополнительных зависимостей для GUI. Визуальная стилизация выполнена в стиле промышленных контроллеров FANUC iPendant — тёмная цветовая схема с высококонтрастными индикаторами состояния."));

  c.push(h.heading3("Архитектура графического интерфейса"));

  c.push(h.p("Главное окно приложения RobotControlGUI (app/views/main_window.py, 1708 строк) реализует паттерн Composite, агрегируя шесть специализированных панелей. Каждая панель является самостоятельным компонентом, отвечающим за определённый аспект управления роботом. Панели взаимодействуют между собой исключительно через шину событий EventBus, что обеспечивает слабую связанность и возможность независимой разработки."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 2.6 — Панели графического интерфейса"));
  c.push(h.makeTable(
    ["Панель", "Файл", "LOC", "Назначение"],
    [
      ["Главное окно", "main_window.py", "1708", "Каркас, меню, вкладки, обработка жизненного цикла"],
      ["Ручное управление", "manual_control_panel.py", "339", "Jogging суставов, слайдер скорости, Home/Center"],
      ["3D кинематика", "kinematics_3d_panel.py", "1100", "3D-визуализация, IK-ввод, маршрутные точки"],
      ["Блочное программирование", "block_programming.py", "254", "Палитра блоков, холст программы, запуск/стоп"],
      ["Маппинг моторов", "motor_mapping_panel.py", "277", "Конфигурация сопоставления суставов и моторов"],
      ["Мониторинг", "bottom_monitor_panel.py", "218", "Телеметрия 6 моторов в реальном времени"],
    ],
    [2200, 2200, 800, 3800]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Как видно из таблицы 2.6, наибольший объём кода приходится на главное окно (1708 LOC) и панель 3D-кинематики (1100 LOC). Главное окно содержит логику инициализации всех подсистем, обработку подключения/отключения оборудования, управление таймерами обновления и корректное завершение приложения. Панель 3D-кинематики включает встроенный 3D-рендерер на базе Matplotlib с интерактивным вращением и масштабированием модели робота."));

  // --- Цветовая схема ---
  c.push(h.heading3("Цветовая схема FANUC"));

  c.push(h.p("Цветовая схема интерфейса разработана на основе анализа промышленных контроллеров FANUC серий R-30iB и CRX. Основные цвета определены в модуле app/config/constants.py и применяются единообразно во всех панелях:"));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 2.7 — Цветовая схема интерфейса"));
  c.push(h.makeTable(
    ["Элемент", "Цвет (HEX)", "Назначение"],
    [
      ["Фон приложения", "#1a1a2e", "Основной фон (тёмно-синий)"],
      ["Фон панелей", "#16213e", "Фон вкладок и панелей"],
      ["Статус OK", "#00ff88", "Зелёный — нормальная работа"],
      ["Предупреждение", "#ff9500", "Оранжевый — параметр выше нормы"],
      ["Ошибка", "#ff4444", "Красный — критическая ситуация"],
      ["Текст основной", "#e0e0e0", "Светло-серый текст"],
      ["Акцент", "#0f3460", "Выделение активных элементов"],
    ],
    [2400, 1800, 4800]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Выбор тёмной цветовой схемы обусловлен условиями эксплуатации: при работе в лабораторных и промышленных условиях тёмный интерфейс снижает утомляемость глаз оператора при длительном наблюдении за показателями телеметрии. Высококонтрастные индикаторы состояния (зелёный/оранжевый/красный) обеспечивают мгновенное визуальное распознавание аномалий."));

  // --- Вкладка ручного управления ---
  c.push(h.heading3("Панель ручного управления"));

  c.push(h.p("Панель ручного управления (ManualControlPanel, 339 LOC) предоставляет оператору интерфейс для прямого управления каждым суставом робота. Интерфейс содержит: выбор активного сустава (J1–J6) через набор радиокнопок, кнопки хода «+» и «−» для пошагового перемещения, слайдер скорости (1–100% от максимальной), команды быстрого позиционирования Home (все суставы в 0°), Center (центральные позиции 2048), а также переключатели включения/отключения момента (Torque ON/OFF) для каждого мотора индивидуально."));

  c.push(h.p("Скорость движения вычисляется как процент от максимальной скорости сервопривода ST3215 (3400 шагов/с). При работе в безопасном режиме скорость дополнительно ограничивается значением 100 шагов/с (≈4% от максимума), что соответствует рекомендациям по безопасности ISO 10218 для режима ручного обучения."));

  // --- 3D визуализация ---
  c.push(h.heading3("Панель 3D-визуализации кинематики"));

  c.push(h.p("Панель 3D-кинематики (Kinematics3DPanel, 1100 LOC) является наиболее сложным компонентом GUI. Она объединяет интерактивную 3D-визуализацию робота, ввод углов суставов с мгновенным обновлением модели, ввод целевых координат (X, Y, Z) с автоматическим решением обратной кинематики и систему маршрутных точек (waypoints) для последовательного перемещения."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.4 — Главное окно Desktop-приложения"));
  c.push(h.emptyLine());

  c.push(h.p("Как показано на рисунке 2.4, 3D-визуализация реализована через встраивание холста Matplotlib (FigureCanvasTkAgg) непосредственно в окно Tkinter. Модель робота отображается как последовательность цветных сегментов (звеньев), соединяющих узловые точки суставов. Каждое звено окрашено в свой цвет для наглядного различения. Конечный эффектор отмечен специальным маркером. Визуализация поддерживает интерактивное вращение (левая кнопка мыши), масштабирование (колесо мыши) и панорамирование (правая кнопка)."));

  c.push(h.codeCaption("Листинг 2.8 — Обновление 3D-визуализации робота"));
  c.push(...h.codeBlock([
    "def _update_3d_plot(self, angles_deg):",
    "    positions = self.kinematics.forward_kinematics(angles_deg)",
    "    self.ax.clear()",
    "    self.ax.set_xlim(-350, 350)",
    "    self.ax.set_ylim(-350, 350)",
    "    self.ax.set_zlim(-100, 400)",
    "    colors = ['#ff4444', '#ff9500', '#00ff88', '#4488ff', '#ff44ff', '#ffff44']",
    "    for i in range(len(positions) - 1):",
    "        p1, p2 = positions[i], positions[i + 1]",
    "        self.ax.plot3D([p1[0], p2[0]], [p1[1], p2[1]], [p1[2], p2[2]],",
    "                       color=colors[i], linewidth=3)",
    "    # Маркер конечного эффектора",
    "    ee = positions[-1]",
    "    self.ax.scatter(*ee, color='red', s=100, marker='o')",
    "    self.canvas.draw_idle()  # неблокирующее обновление",
  ]));
  c.push(h.emptyLine());

  c.push(h.p("Метод draw_idle (листинг 2.8) используется вместо draw для обеспечения плавного обновления без блокировки основного потока Tkinter. Это особенно важно при быстром перемещении слайдеров углов, когда обновления происходят с частотой до 30 Гц."));

  // --- Блочное программирование ---
  c.push(h.heading3("Панель блочного программирования"));

  c.push(h.p("Панель блочного программирования (BlockProgrammingPanel, 254 LOC) предоставляет визуальный интерфейс для составления последовательностей действий робота без написания кода. Палитра содержит следующие типы блоков: движение сустава (указание сустава, угла и скорости), движение к точке XYZ (через IK), ожидание (пауза в секундах), управление моментом (включение/отключение для указанного мотора) и команда Home (возврат в нулевую позицию)."));

  c.push(h.p("Оператор перетаскивает блоки из палитры на холст программы, формируя последовательность шагов. Программа может быть запущена (последовательное выполнение всех блоков), поставлена на паузу (остановка между блоками) и остановлена (немедленное прерывание текущего блока через emergency_stop). Составленная программа сохраняется в формате JSON и может быть загружена повторно."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.5 — Панель блочного программирования"));
  c.push(h.emptyLine());

  // --- Мониторинг ---
  c.push(h.heading3("Панель мониторинга"));

  c.push(h.p("Нижняя панель мониторинга (BottomMonitorPanel, 218 LOC) отображает телеметрию всех шести сервоприводов в реальном времени. Для каждого мотора отображаются: текущая позиция (0–4095 и эквивалент в градусах), температура (°C) с цветовой индикацией по порогам, напряжение питания (В), ток потребления (мА), нагрузка (%) и вычисленный статус (OK/WARNING/ERROR). Данные обновляются с частотой 2 Гц через подписку на события EventBus."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.6 — Панель мониторинга телеметрии"));
  c.push(h.emptyLine());

  c.push(h.p("Цветовая индикация статуса мотора (рисунок 2.6) обеспечивает мгновенное визуальное обнаружение аномалий: зелёный фон (#00ff88) означает нормальную работу, оранжевый (#ff9500) — предупреждение о приближении к предельным значениям, красный (#ff4444) — критическую ситуацию, требующую немедленного вмешательства оператора. При обнаружении состояния ERROR панель мониторинга дополнительно воспроизводит звуковой сигнал через системный beep."));

  c.push(h.pageBreak());
  return c;
};
