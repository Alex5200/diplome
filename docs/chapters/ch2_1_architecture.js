/**
 * ГЛАВА 2.1 — Архитектура и паттерны проектирования
 *
 * Содержание:
 *   - Общая многоплатформенная архитектура (shared → Desktop/Web/ROS2)
 *   - Паттерны: Dependency Injection, Event Bus, Service Layer
 *   - Структура директорий проекта
 *   - UML: компонентная диаграмма системы
 *   - Диаграмма потоков данных
 *
 * Нумерация: таблицы 2.1–2.3, рисунки 2.1–2.3, формулы нет
 * Целевой объём: ~3–4 страницы
 */
module.exports = function (h) {
  const c = [];

  c.push(h.heading1("ГЛАВА 2. РЕАЛИЗАЦИЯ ПРОГРАММНОГО ОБЕСПЕЧЕНИЯ"));

  c.push(h.heading2("2.1 Архитектура и паттерны проектирования"));

  c.push(h.p("Проектирование программного обеспечения для управления роботом-манипулятором требует сбалансированного подхода, сочетающего гибкость развёртывания на различных платформах с единообразием алгоритмического ядра. В рамках данной работы была разработана многоплатформенная архитектура, разделяющая платформо-независимое ядро и три клиентских слоя: Desktop (локальный графический интерфейс на базе Tkinter), Web (браузерный интерфейс на базе FastAPI) и ROS2 (интеграция с экосистемой Robot Operating System). Такой подход обеспечивает возможность использования одного и того же алгоритмического кода кинематики, управления моторами и мониторинга на любой платформе без дублирования логики."));

  c.push(h.p("Принцип разделения ответственности (Separation of Concerns) реализован на уровне директорий проекта. Общий слой, размещённый в директории app/, содержит все модели данных, контроллеры оборудования, сервисы бизнес-логики, инфраструктуру событий и утилиты конфигурации. Каждый клиентский слой импортирует общий код через пакет shared/, который реэкспортирует публичные символы. Такая схема позволяет в перспективе вынести общий слой в самостоятельный pip-пакет без изменения клиентских слоёв, что является важным преимуществом для масштабирования системы."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 2.1 — Многоплатформенная структура проекта"));
  c.push(h.makeTable(
    ["Слой", "Директория", "Технологии", "Назначение"],
    [
      ["Общий (shared)", "app/", "Python 3.12, NumPy", "Модели данных, кинематика, контроллеры, сервисы, события"],
      ["Desktop", "desktop/", "Tkinter, Matplotlib", "GUI в стиле FANUC, локальное подключение по USB"],
      ["Web", "web/", "FastAPI, WebSocket", "REST API + WebSocket + HTML/JS браузерный клиент"],
      ["ROS2", "ros2/", "rclpy, ament_python", "Пакет robot_control: три ноды для интеграции с ROS2"],
      ["Программы", "programs/", "Python, argparse", "Высокоуровневые задачи: захват, рисование, калибровка"],
      ["Симуляция", "mujoco/", "MuJoCo, Gymnasium", "Цифровой двойник робота для обучения с подкреплением"],
    ],
    [1600, 1400, 1800, 4200]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Как видно из таблицы 2.1, проект состоит из шести функциональных слоёв. Общий слой является фундаментом системы и содержит более 2200 строк кода, реализующих модели данных, контроллеры оборудования и сервисы бизнес-логики. Каждый клиентский слой добавляет специфичную для платформы логику представления и ввода-вывода."));

  // --- Паттерны ---
  c.push(h.heading3("Паттерн Dependency Injection"));

  c.push(h.p("Для обеспечения слабой связанности модулей и упрощения тестирования в проекте реализован контейнер зависимостей DependencyContainer (app/core/container.py). Контейнер обеспечивает централизованную регистрацию и разрешение зависимостей по типу интерфейса. Все ключевые компоненты системы — MotorController, KinematicsService, RobotService, ProgramService — зарегистрированы в контейнере как синглтоны. При создании сервиса контейнер автоматически внедряет все необходимые зависимости через конструктор, что избавляет от необходимости ручного создания и передачи объектов."));

  c.push(h.p("Применение данного паттерна обеспечивает ряд преимуществ: возможность подмены реальных компонентов mock-объектами при тестировании, прозрачное управление жизненным циклом сервисов и отсутствие прямых зависимостей между модулями. В модуле app/core/interfaces.py определены абстрактные интерфейсы IMotorController, IMotorMonitor и IService, от которых наследуют конкретные реализации."));

  c.push(h.codeCaption("Листинг 2.1 — Абстрактный интерфейс контроллера моторов"));
  c.push(...h.codeBlock([
    "class IMotorController(ABC):",
    "    @abstractmethod",
    "    def connect(self, port: str, baudrate: int) -> bool: ...",
    "    @abstractmethod",
    "    def disconnect(self) -> None: ...",
    "    @abstractmethod",
    "    def move_joint(self, joint: int, position: int, speed: int) -> None: ...",
    "    @abstractmethod",
    "    def emergency_stop(self) -> None: ...",
    "    @abstractmethod",
    "    def get_motor_data(self, motor_id: int) -> MotorData: ...",
  ]));
  c.push(h.emptyLine());

  c.push(h.p("Листинг 2.1 демонстрирует абстрактный интерфейс IMotorController, определяющий контракт для всех реализаций контроллера моторов. Данный интерфейс наследуется классом MotorController для работы с реальным оборудованием ST3215 и классом MockMotorController для юнит-тестирования."));

  // --- Event Bus ---
  c.push(h.heading3("Паттерн Event Bus (Publish–Subscribe)"));

  c.push(h.p("Для обмена данными между модулями без создания прямых зависимостей реализована шина событий EventBus (app/core/events.py). Шина работает по паттерну publish–subscribe: компоненты подписываются на определённые типы событий, а при наступлении события все подписчики получают уведомление. Данный механизм является ключевым для организации потока телеметрических данных: MotorMonitor публикует обновления состояния моторов, а панели GUI, WebSocket-транслятор и ROS2-ноды подписываются на эти обновления независимо друг от друга."));

  c.push(h.p("Реализация EventBus поддерживает типизированные события, синхронные и асинхронные обработчики, а также механизм отписки для предотвращения утечек памяти при закрытии компонентов. Типичный жизненный цикл события: MotorMonitor опрашивает моторы → формирует объект MotorData → публикует событие motor_update через EventBus → все подписчики (GUI-панели, WebSocket, логгер) обрабатывают данные в своём контексте."));

  // --- Service Layer ---
  c.push(h.heading3("Паттерн Service Layer"));

  c.push(h.p("Бизнес-логика приложения инкапсулирована в слое сервисов, наследующих абстрактный класс BaseService (app/core/base_service.py). BaseService определяет стандартный жизненный цикл: initialize → start → stop. На этапе initialize сервис получает зависимости из контейнера и подготавливает внутреннее состояние. Метод start запускает фоновые потоки и таймеры. Метод stop корректно завершает все фоновые задачи и освобождает ресурсы. Такой унифицированный подход упрощает управление временем жизни компонентов при старте и завершении приложения."));

  c.push(h.p("В системе реализованы три основных сервиса: KinematicsService кэширует результаты прямой и обратной кинематики для снижения вычислительной нагрузки и проверяет досягаемость целевых точек; RobotService синхронизирует состояние робота между контроллером и GUI; ProgramService управляет загрузкой, выполнением и остановкой высокоуровневых программ-задач."));

  // --- Диаграмма ---
  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.1 — Компонентная диаграмма системы (UML)"));
  c.push(h.emptyLine());

  c.push(h.p("На рисунке 2.1 представлена компонентная UML-диаграмма системы, отражающая связи между основными модулями. Стрелки зависимости направлены от клиентских слоёв к общему ядру, что подтверждает соблюдение принципа инверсии зависимостей (Dependency Inversion Principle): высокоуровневые модули GUI и Web зависят от абстракций (интерфейсов), а не от конкретных реализаций."));

  // --- Поток данных ---
  c.push(h.heading3("Поток данных в системе"));

  c.push(h.p("Поток данных в системе организован по двунаправленной схеме «команда → исполнение → обратная связь». Командный поток: пользователь инициирует действие через интерфейс платформы (кнопка в GUI, REST-запрос, ROS2-топик) → платформа вызывает метод сервиса → сервис делегирует операцию контроллеру → MotorController формирует UART-пакет → пакет передаётся на сервопривод. Обратный поток: MotorMonitor опрашивает сервоприводы каждые 0.5 секунды → обновляет объект MotorData → публикует событие через EventBus → обработчики платформы обновляют отображение."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.2 — Диаграмма потоков данных"));
  c.push(h.emptyLine());

  c.push(h.p("Рисунок 2.2 иллюстрирует потоки данных между компонентами системы. Особенностью архитектуры является полная асинхронность обратного потока: телеметрия собирается в фоновом потоке и доставляется подписчикам без блокировки основного потока управления. Это обеспечивает отзывчивость интерфейса даже при интенсивном мониторинге шести сервоприводов одновременно."));

  // --- Структура общего слоя ---
  c.push(h.heading3("Структура общего слоя (app/)"));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 2.2 — Структура общего слоя приложения"));
  c.push(h.makeTable(
    ["Пакет", "Модули", "LOC", "Назначение"],
    [
      ["core/", "interfaces, base_service, events, container", "~460", "Фреймворк: DI, события, абстракции, жизненный цикл"],
      ["models/", "kinematics, motor_data", "~930", "Модели: DH-кинематика, MotorData, JointState, RobotState"],
      ["controllers/", "motor_controller, motor_monitor", "~330", "Контроллеры: UART-управление ST3215, мониторинг"],
      ["services/", "robot_service, kinematics_service, program_service", "~880", "Сервисы: состояние робота, кэш кинематики, программы"],
      ["utils/", "config_manager, logger, program_executor", "~350", "Утилиты: конфигурация, логирование, исполнитель"],
      ["config/", "constants", "~80", "Константы: цвета, пороги, ограничения"],
    ],
    [1600, 3200, 800, 3400]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Таблица 2.2 показывает детальную структуру общего слоя. Суммарный объём составляет более 3000 строк Python-кода. Наибольший объём приходится на модели данных (930 LOC), что объясняется сложностью реализации кинематических алгоритмов с полным набором DH-преобразований, и на сервисный слой (880 LOC), обеспечивающий координацию между компонентами."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.3 — UML-диаграмма классов общего слоя"));
  c.push(h.emptyLine());

  c.push(h.p("UML-диаграмма классов (рисунок 2.3) отражает наследование от абстрактных интерфейсов и связи агрегации между компонентами. Центральным элементом является RobotService, агрегирующий MotorController, KinematicsService и EventBus. Диаграмма наглядно демонстрирует соблюдение принципов SOLID: каждый класс имеет единственную ответственность (SRP), зависимости направлены от конкретных реализаций к абстракциям (DIP), а интерфейсы минимальны и специализированы (ISP)."));

  c.push(h.pageBreak());
  return c;
};
