/**
 * ГЛАВА 2.5 — Web интерфейс и REST API
 *
 * Содержание:
 *   - Архитектура FastAPI-сервера
 *   - REST API endpoints
 *   - WebSocket для реального времени
 *   - Веб-клиент (HTML/JS SPA)
 *   - Скриншоты web-интерфейса
 *
 * Нумерация: таблицы 2.8–2.9, рисунки 2.7–2.8, листинги 2.10–2.12
 * Целевой объём: ~2–3 страницы
 */
module.exports = function (h) {
  const c = [];

  c.push(h.heading2("2.5 Web-интерфейс и REST API"));

  c.push(h.p("Web-слой (директория web/) обеспечивает удалённый доступ к роботу-манипулятору через браузер без установки дополнительного программного обеспечения на клиентском устройстве. Сервер реализован на фреймворке FastAPI (Python 3.12+), обеспечивающем автоматическую генерацию OpenAPI-документации, валидацию входных данных через Pydantic-модели и высокую производительность благодаря асинхронной обработке запросов на базе ASGI-сервера Uvicorn."));

  c.push(h.heading3("Архитектура серверной части"));

  c.push(h.p("Серверная часть web-слоя состоит из трёх компонентов: точки входа (web/main.py), модуля REST API (web/api/routes.py) и модуля WebSocket (web/api/websocket.py). Точка входа создаёт экземпляр FastAPI-приложения, подключает маршруты API и настраивает раздачу статических файлов для веб-клиента. Серверная часть использует тот же общий слой (app/), что и Desktop-приложение, обеспечивая идентичное поведение алгоритмов на обеих платформах."));

  c.push(h.emptyLine());
  c.push(h.tableCaption("Таблица 2.8 — REST API endpoints"));
  c.push(h.makeTable(
    ["Метод", "Путь", "Тело запроса", "Описание"],
    [
      ["GET", "/api/status", "—", "Текущее состояние робота и всех моторов"],
      ["POST", "/api/connect", "{port, baudrate}", "Подключение к COM-порту"],
      ["POST", "/api/disconnect", "—", "Отключение от COM-порта"],
      ["POST", "/api/move", "{joint, angle, speed}", "Движение указанного сустава"],
      ["POST", "/api/move_all", "{angles[], speed}", "Движение всех суставов одновременно"],
      ["POST", "/api/stop", "—", "Аварийная остановка всех моторов"],
      ["POST", "/api/ik", "{x, y, z}", "Решение обратной кинематики для точки"],
      ["GET", "/api/config", "—", "Текущая конфигурация маппинга моторов"],
    ],
    [1000, 1800, 2200, 4000]
  ));
  c.push(h.emptyLine());

  c.push(h.p("Таблица 2.8 содержит полный перечень REST API endpoints. Все endpoints возвращают ответ в формате JSON. Для endpoints, модифицирующих состояние робота (POST), реализована валидация входных данных через Pydantic-модели. В случае ошибки валидации возвращается HTTP 422 с детальным описанием ошибки, сгенерированным автоматически фреймворком FastAPI."));

  c.push(h.codeCaption("Листинг 2.10 — Реализация endpoint решения обратной кинематики"));
  c.push(...h.codeBlock([
    "@router.post('/api/ik')",
    "async def solve_ik(request: IKRequest):",
    "    \"\"\"Решить IK для целевой точки (x, y, z).\"\"\"",
    "    result = kinematics_service.solve_ik(request.x, request.y, request.z)",
    "    if result is None:",
    "        raise HTTPException(",
    "            status_code=400,",
    "            detail=f'Точка ({request.x}, {request.y}, {request.z}) недосягаема'",
    "        )",
    "    return {",
    "        'angles': result,",
    "        'position': kinematics_service.forward_kinematics(result),",
    "        'error_mm': kinematics_service.compute_error(result, request.x, request.y, request.z)",
    "    }",
  ]));
  c.push(h.emptyLine());

  c.push(h.p("Листинг 2.10 показывает реализацию наиболее вычислительно сложного endpoint — решения обратной кинематики. Запрос содержит целевые координаты (x, y, z) в миллиметрах. В случае недосягаемости точки возвращается HTTP 400 с информативным сообщением. В случае успеха ответ включает найденные углы суставов, позицию конечного эффектора (для верификации) и ошибку позиционирования в миллиметрах."));

  // --- WebSocket ---
  c.push(h.heading3("WebSocket для передачи телеметрии в реальном времени"));

  c.push(h.p("Для передачи телеметрических данных в реальном времени реализован WebSocket-endpoint (WS /ws). В отличие от REST API, где клиент инициирует каждый запрос, WebSocket обеспечивает двунаправленный канал связи: сервер проактивно отправляет обновления телеметрии без опроса со стороны клиента. Частота обновлений составляет 5 Гц (200 мс между кадрами), что обеспечивает плавное отображение показателей в браузере."));

  c.push(h.codeCaption("Листинг 2.11 — WebSocket-транслятор телеметрии"));
  c.push(...h.codeBlock([
    "class WebSocketManager:",
    "    def __init__(self):",
    "        self.active_connections: List[WebSocket] = []",
    "",
    "    async def broadcast(self, data: dict):",
    "        disconnected = []",
    "        for ws in self.active_connections:",
    "            try:",
    "                await ws.send_json(data)",
    "            except WebSocketDisconnect:",
    "                disconnected.append(ws)",
    "        for ws in disconnected:",
    "            self.active_connections.remove(ws)",
  ]));
  c.push(h.emptyLine());

  c.push(h.p("Реализация WebSocket-менеджера (листинг 2.11) поддерживает одновременное подключение нескольких клиентов. Каждое обновление телеметрии отправляется всем активным соединениям (паттерн broadcast). Отключившиеся клиенты автоматически удаляются из списка при попытке отправки. Данные передаются в формате JSON с полями: motor_id, position, temperature, voltage, current, load, status."));

  // --- Web-клиент ---
  c.push(h.heading3("Веб-клиент (одностраничное приложение)"));

  c.push(h.p("Клиентская часть web-интерфейса (web/static/index.html) реализована как одностраничное приложение (SPA) на чистом HTML, CSS и JavaScript без использования фреймворков (React, Vue и т.д.). Такое решение минимизирует размер загрузки (менее 50 КБ) и исключает необходимость установки Node.js и сборщиков модулей. Клиент содержит следующие функциональные блоки: панель подключения (выбор COM-порта и baudrate), ручное управление суставами (6 слайдеров с индикацией текущей позиции), форму для решения IK (ввод координат X, Y, Z), таблицу телеметрии в реальном времени (обновляемую через WebSocket) и журнал событий с временными метками."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.7 — Web-интерфейс управления роботом"));
  c.push(h.emptyLine());

  c.push(h.p("На рисунке 2.7 представлен web-интерфейс в браузере. Адаптивная вёрстка обеспечивает корректное отображение на устройствах с различной шириной экрана — от мобильных телефонов (320 пикселей) до настольных мониторов (1920 пикселей и более). Это позволяет оператору контролировать робота с планшета или смартфона, находясь непосредственно рядом с манипулятором."));

  c.push(h.emptyLine());
  c.push(...h.figurePlaceholder("Рисунок 2.8 — Автоматическая документация Swagger UI"));
  c.push(h.emptyLine());

  c.push(h.p("Дополнительным преимуществом использования FastAPI является автоматическая генерация интерактивной документации Swagger UI (рисунок 2.8), доступной по адресу /docs. Документация позволяет разработчикам тестировать все API-endpoints непосредственно из браузера, формируя и отправляя запросы с произвольными параметрами. Для каждого endpoint автоматически генерируется описание входных и выходных данных на основе Pydantic-моделей."));

  c.push(h.pageBreak());
  return c;
};
